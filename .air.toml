tmp_dir = "build"
rum_cmd = "build/server"

[build]
    cmd = "make build"
    bin = "./build/server"
    exclude_regex = ["_test.go", ".*_templ.go", "build/.*"]
    include_ext = ["go", "tpl", "tmpl", "templ", "html", "css"]
